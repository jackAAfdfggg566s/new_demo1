你这个需求很常见，用 sh 读取 .cfg（一般是 key=value 格式）里的某个字段，可以有几种写法，按你场景给你最实用的👇
✅ 方法1：用 grep + cut（最简单直接）
假设 cfg 内容：
Cfg
username=jack
password=123456
host=localhost
读取 username：
Bash
username=$(grep '^username=' config.cfg | cut -d'=' -f2)
echo "$username"
✔ 优点：简单
❗ 注意：如果有空格或重复 key 会有问题
✅ 方法2：用 grep + sed（更稳一点）
Bash
username=$(grep '^username=' config.cfg | sed 's/^username=//')
echo "$username"
✅ 方法3：用 awk（推荐，干净）
Bash
username=$(awk -F= '/^username=/{print $2}' config.cfg)
echo "$username"
✔ 优点：
只匹配一行
结构清晰
可扩展
✅ 方法4：直接 source（最方便，但有坑⚠️）
如果 cfg 完全符合 shell 变量格式（没有空格、特殊字符）：
Bash
source config.cfg
echo "$username"
或者：
Bash
. config.cfg
❗ 风险：
文件里如果有奇怪内容会执行（安全风险）
不能有 key = value（有空格会报错）
✅ 方法5：处理带空格的情况（更严谨）
如果 cfg 是：
Cfg
username = jack
用这个：
Bash
username=$(awk -F '=' '/^username/{gsub(/ /,"",$2); print $2}' config.cfg)
🚀 推荐你用哪个？
如果你是 GitLab pipeline / 自动化脚本：
👉 优先用：
Bash
username=$(awk -F= '/^username=/{print $2}' config.cfg)
稳定 + 不会误执行文件
⚡ 进阶：写成函数（通用）
Bash
get_cfg() {
  key=$1
  file=$2
  awk -F= -v k="$key" '$1==k {print $2}' "$file"
}

username=$(get_cfg username config.cfg)
如果你把你的 .cfg 文件贴出来（比如有没有 [section]、空格、引号），我可以帮你写一个完全适配你项目的版本，避免踩坑。