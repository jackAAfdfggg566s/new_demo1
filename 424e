你现在想做的是：
👉 不再“整页截图”，而是
👉 根据关键字，在日志里定位位置 → 只截那一段
这个思路完全对，而且比整页截图更实用 👍
我给你一个从日志 → HTML → 精准截图的完整方案（你之前用过 Flask + Playwright，这个正好适配）。
✅ ✅ 核心思路（3步）
1️⃣ 找关键字在日志里的位置
2️⃣ 转成 HTML（或已有 HTML）
3️⃣ 用浏览器滚动到位置截图
✅ 方案一（推荐）：Playwright 精准定位截图
使用 Playwright
🔥 示例代码（精准截图关键字）
Python
from playwright.sync_api import sync_playwright

KEYWORD = "ERROR"

with sync_playwright() as p:
    browser = p.chromium.launch()
    page = browser.new_page()

    # 👉 打开你的日志HTML（可以是本地或URL）
    page.goto("http://your-log-page")

    # 👉 找到关键字元素
    locator = page.locator(f"text={KEYWORD}")

    if locator.count() > 0:
        element = locator.first

        # 👉 滚动到该位置
        element.scroll_into_view_if_needed()

        # 👉 截图（只截这一块）
        element.screenshot(path="keyword.png")
    else:
        print("Keyword not found")

    browser.close()
✅ 方案二：截“关键字附近区域”（更实用🔥）
只截关键字太小，可以截“上下文”
Python
box = element.bounding_box()

page.screenshot(
    path="context.png",
    clip={
        "x": box["x"],
        "y": box["y"] - 200,   # 上方多截一点
        "width": box["width"],
        "height": box["height"] + 400  # 上下扩展
    }
)
✅ 方案三：如果你只有“纯日志文件”（没有HTML）
1️⃣ 转 HTML（简单版）
Python
with open("log.txt") as f:
    content = f.read()

html = f"<pre>{content}</pre>"

with open("log.html", "w") as f:
    f.write(html)
2️⃣ 再用 Playwright 打开
Python
page.goto("file:///path/to/log.html")
✅ 方案四（你之前 Flask 场景）
你之前说有 endpoint，例如：
Plain text
http://server/log?filename=xxx
👉 那直接：
Python
page.goto("http://server/log?filename=xxx")
👉 然后同样：
Python
locator = page.locator(f"text={KEYWORD}")
⚠️ 常见坑（你大概率遇到过）
❌ 1. 关键字找不到
Python
locator.count() == 0
👉 可能原因：
页面没加载完 → 加：
Python
page.wait_for_timeout(2000)
❌ 2. 日志太长，没渲染
👉 有些页面是懒加载
解决：
Python
page.evaluate("window.scrollTo(0, document.body.scrollHeight)")
❌ 3. 截图位置偏了
👉 bounding_box 可能为 None
加判断：
Python
if box:
    ...
🚀 更高级（你可以直接用）
✔ 找多个关键字并截图
Python
locators = page.locator(f"text={KEYWORD}")

for i in range(locators.count()):
    el = locators.nth(i)
    el.screenshot(path=f"keyword_{i}.png")
✔ 用正则匹配（日志更常见）
Python
locator = page.locator("text=/ERROR|FAIL|Exception/")
✅ 总结一句话
👉 用 Playwright 定位关键字元素 → scroll → element.screenshot() = 精准截图
🚀 如果你想一步到位（更自动化）
你可以做到：
👉 pipeline 执行 → 远端生成 log → Flask 提供 → Playwright 截图 → 自动写 Word 报告
（你之前已经在做这个了 👍）
如果你把你现在的：
日志格式（几行）
或 HTML 页面结构
贴出来，我可以帮你👇
👉 写一个“完全匹配你日志结构”的截图脚本（保证定位不偏 + 一次成功）