可以，用 Python 很容易把一个目录下的所有图片批量插入到 Word（.docx）文档里。最常用的是用 python-docx 这个库。
下面给你一个完整可用的示例 👇
1️⃣ 安装依赖
Bash
pip install python-docx
2️⃣ 示例代码（批量插入图片）
Python
import os
from docx import Document
from docx.shared import Inches

# 图片目录
image_folder = "images"   # 改成你的目录路径

# 创建 Word 文档
doc = Document()

# 支持的图片格式
image_exts = ('.png', '.jpg', '.jpeg', '.bmp', '.gif')

# 遍历目录
for filename in os.listdir(image_folder):
    if filename.lower().endswith(image_exts):
        img_path = os.path.join(image_folder, filename)

        # 添加图片标题（可选）
        doc.add_paragraph(filename)

        # 插入图片（宽度可调）
        doc.add_picture(img_path, width=Inches(5))

        # 每张图后换行
        doc.add_page_break()

# 保存文档
doc.save("output.docx")
3️⃣ 常见优化（你大概率会用到）
✅ 1. 按文件名排序
Python
for filename in sorted(os.listdir(image_folder)):
✅ 2. 不分页（连续排）
把这行删掉：
Python
doc.add_page_break()
✅ 3. 控制图片大小（很重要）
Python
doc.add_picture(img_path, width=Inches(4))
或者：
Python
doc.add_picture(img_path, height=Inches(3))
✅ 4. 子目录递归（如果图片分散）
Python
for root, dirs, files in os.walk(image_folder):
    for filename in files:
        if filename.lower().endswith(image_exts):
            img_path = os.path.join(root, filename)
⚠️ 注意点
Word 实际格式是 .docx（不是旧的 .doc）
图片太多 → 文件会很大
超大图片建议先压缩，否则 Word 会卡
如果你要做“测试报告那种”
你之前说要自动截图 + Word报告，其实可以进一步升级成：
图片标题 = 测试步骤
图片说明 = 日志关键字
自动编号（图1、图2…）
自动生成目录
我可以帮你做一个完整自动报告生成模板（带截图+Word），直接接你现在的 pipeline。