是合理且常见的，但要区分是哪一层在拒绝（control plane / network / security / client）。你这个现象是：
RDS failover 已完成（通常 30–120 秒）
但 VPC Flow Log 持续 30 分钟出现 REJECT 5432
应用在这段时间无法连接
这说明问题已经超出数据库切换本身，而是网络路径或连接目标异常。
下面分层拆解原因。
一、先明确：Flow Log 的 REJECT 表示什么？
在 Amazon Web Services 的 VPC Flow Log 里：
复制代码

action = REJECT
dstport = 5432
表示：
流量在 VPC 层被拒绝（Security Group / NACL）
而不是数据库进程拒绝。
如果是数据库没 ready，Flow Log 仍然是 ACCEPT，只是 TCP 连接超时或 RST。
所以你看到 REJECT，说明是网络层拦截。
二、RDS failover 正常时间线
以 Amazon RDS（PostgreSQL）为例：
主实例 crash / AZ 失效
standby promote（30–60 秒）
DNS 切换到新 IP
客户端重新解析 DNS
建立新 TCP 连接（5432）
理论上 1–2 分钟完成。
所以 30 分钟异常一定是别的东西。
三、最常见 4 个真实原因
① Security Group 指向了旧 ENI
Failover 后：
RDS 会创建 / 绑定新的 ENI
新 ENI 可能属于不同 subnet / 不同 AZ
如果你：
应用 SG 只允许旧 ENI / 旧 subnet
或 DB SG 只允许旧 source
就会：
复制代码

新流量 -> 新 ENI -> 被 SG 拒绝 -> Flow log REJECT
📌 特征：
REJECT 来自 DB subnet ENI
端口 5432
直到手动修改 SG 才恢复
② NACL 不对称规则
Network ACL 是 stateless。
如果：
inbound 允许 5432
outbound ephemeral port 没开（1024–65535）
就会：
复制代码

SYN -> OK
SYN-ACK -> 被拒绝
Flow log 会显示 REJECT。
Failover 到不同 subnet 时更容易触发，因为不同 subnet 可能 NACL 不一致。
③ 应用还在访问旧 IP（DNS 缓存问题）
Failover 后 DNS 变更。
如果：
Java 默认 DNS cache TTL = 无限
或 node resolver 缓存 30 分钟
应用可能仍访问：
复制代码

旧 RDS IP
而旧 ENI 可能：
已经 detached
或 security group 不再允许
于是 VPC 层 REJECT。
📌 这个现象特别符合“刚好 30 分钟”。
很多 JVM 默认 DNS TTL 30 分钟。
④ RDS Proxy + subnet 故障
你之前说过有 RDS Proxy。
如果：
Proxy subnet 在 failover AZ 里断了 30 分钟
Proxy 无法连后端
连接在 proxy 层被拒绝
Flow log 也会显示 5432 REJECT。
尤其当：
EKS 所在 subnet 与 DB subnet 网络中断
四、为什么刚好 30 分钟？
30 分钟在 AWS 里非常典型：
组件
默认缓存时间
JVM DNS cache
30 min
Linux nscd
30 min
CoreDNS cache
30 min
某些 LB health cache
30 min
如果问题 30 分钟后自动恢复，99% 是 DNS 或 subnet 网络恢复。
五、怎么精准定位？
建议按这个顺序排查：
1️⃣ 查 REJECT 来自哪个 ENI
复制代码

fields interface-id, srcaddr, dstaddr, action, dstport
| filter dstport=5432 and action="REJECT"
| stats count() by interface-id
确认：
是 RDS ENI？
还是 EKS node ENI？
2️⃣ 查当时 RDS 解析 IP
在出问题时：
复制代码

nslookup your-rds-endpoint
看是否仍指向旧 IP。
3️⃣ 查看 failover 记录
在 RDS events 里确认：
failover 完成时间
新 AZ
新 subnet
4️⃣ 对比 subnet
确认：
RDS 新 AZ 的 subnet 是否有不同 NACL
是否 route table 一样
六、什么时候“不合理”？
如果：
RDS event 显示 1 分钟完成
DNS 正确
SG 正确
仍 30 分钟 REJECT
那说明是：
网络路径本身断了（subnet / route / IGW / NAT 故障）
这就不是数据库问题，而是 VPC 级故障。
七、结合你之前的情况（EKS + RDS + failover + subnet 断 30 分钟）
如果你之前说：
某个 AZ subnet 断开 30 分钟
那非常合理：
RDS failover 1 分钟完成
但应用所在 subnet 到 DB subnet 网络断 30 分钟
所以持续 REJECT
数据库其实是好的。
结论
✔ VPC Flow Log 5432 REJECT 持续 30 分钟是合理的
✔ 但根因一定在网络或 DNS，不在 RDS failover 本身
✔ 最常见是 DNS 缓存或 subnet NACL/SG 不一致
如果你愿意，把：
Flow log 一条 sample
RDS failover 时间
REJECT 的 interface-id
是否跨 AZ
发我，我可以帮你精确判断是哪一层的问题。