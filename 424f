这个需求很常见 👍：
👉 既要在 GitLab job 里看到实时日志
👉 又要把完整输出保存成 HTML 用于截图
关键点是：“一边输出到控制台，一边写文件”
✅ ✅ 最简单可用方案（推荐）
👉 用 Linux 的 tee
🔥 示例
Bash
sh run_rt.sh | tee output.log
👉 效果：
GitLab 控制台 ✅ 有输出
同时生成 output.log 文件 ✅
✅ 再转 HTML（关键）
Bash
sh run_rt.sh | tee output.log

# 转 HTML（简单版）
echo "<pre>" > output.html
cat output.log >> output.html
echo "</pre>" >> output.html
🎯 一步到位写法（推荐）
Bash
sh run_rt.sh | tee output.log | sed 's/$/<br>/' > output.html
👉 说明：
tee → 保留日志
sed → 每行加 <br>
直接生成 HTML
✅ GitLab .gitlab-ci.yml 示例
YAML
run_rt:
  stage: test
  script:
    - sh run_rt.sh | tee output.log
    - |
      echo "<html><body><pre>" > output.html
      cat output.log >> output.html
      echo "</pre></body></html>" >> output.html

  artifacts:
    paths:
      - output.log
      - output.html
🚀 更高级（推荐你用）
✔ 带颜色日志（保留格式）
如果你的日志有颜色（ANSI）：
Bash
sh run_rt.sh | tee output.log | ansi2html > output.html
👉 安装：
Bash
pip install ansi2html
✔ Python 版（你后面截图更好控制）
Python
import subprocess

with open("output.log", "w") as f:
    process = subprocess.Popen(
        ["sh", "run_rt.sh"],
        stdout=subprocess.PIPE,
        stderr=subprocess.STDOUT,
        text=True
    )

    lines = []
    for line in process.stdout:
        print(line, end="")   # 👉 GitLab 实时输出
        f.write(line)
        lines.append(line)

# 转 HTML
with open("output.html", "w") as f:
    f.write("<pre>")
    f.writelines(lines)
    f.write("</pre>")
⚠️ 常见坑（你要注意）
❌ 1. 没有实时输出
如果你写：
Bash
output=$(sh run_rt.sh)
👉 ❌ GitLab不会实时显示
❌ 2. stdout / stderr 丢失
必须：
Bash
sh run_rt.sh 2>&1 | tee output.log
👉 把错误也收集进去
❌ 3. HTML 打不开
记得加：
HTML
<html><body><pre>...</pre></body></html>
🚀 和你截图方案结合（关键）
你后面可以直接：
Python
page.goto("file:///.../output.html")
然后：
Python
locator = page.locator("text=ERROR")
👉 完美衔接你之前截图方案 👍
✅ 总结一句话
👉 tee = 同时输出 + 保存日志；再包一层 HTML 就能截图
如果你下一步想做👇
👉 “不同 RT 类型 → 自动截图不同关键字 → 自动生成 Word 报告”
�⁠我可以帮你把整个链路（RT + HTML + Playwright + Word）一次性串起来 👍